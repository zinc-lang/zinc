fn main() {
    1;
    false;
    {
        true;
        return 5;
        return;
    }

    1 + 2;

    three;
    add;
    three();

    let a = 2;
    let b = three();
    let x = add(a, b);
}

fn three(): sint {
    return 1 + 2;
}

fn add(a: sint, b: sint): sint {
    return a + b;
}

fn sum(a: sint, b: sint, c: sint): sint {
    return a + b + c;
}
