use ::self::frontend::syntax::ast::{SyntaxNode, ExpressionNode};
use ::self::frontend::syntax::ast::expr::*;
use super::*;

impl SelectValue {
    fn new(e: *TernaryExpr, cond: *RValue, br_true: *RValue, br_false: *RValue) -> *Self {
        let span = e.span();
        return box {
            .expr = move e,
            .parent_node = None,
            .cond = move cond,
            .br_true = move br_true,
            .br_false = move br_false,
            .sty = SType::sty_invalid(span),
        }: SelectValue;
    }
}

impl XValue for SelectValue {
    fn expr_node(&self) -> *ExpressionNode {
        return self.expr as *ExpressionNode;
    }

    fn parent_scope(&self) -> Option<*ScopeSymbol> {
        return self.parent_node;
    }

    fn set_parent_scope(&mut self, ss: *ScopeSymbol) {
        self.parent_node = Some(move ss);
    }

    fn get_stype(&self) -> *SType {
        return self.sty;
    }

    fn set_stype(&mut self, sty: *SType) {
        self.sty = move sty;
    }

    fn dump(&self) {
        print("if");
        self.cond.dump();
        print(" then ... else ... ");
    }
}

impl RValue for SelectValue {

}
