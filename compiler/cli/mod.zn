pub mod options;
pub mod session;
pub mod timeline;

fn run() {
    let options = options::CliOptions::get();
    options.validate();
    
    let compilation: session::Session = {
        ...
    };
    let exit_code = compilation.compile();
    std::process::exit(exit_code);
}
