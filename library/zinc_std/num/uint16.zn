use ::self::primitives::string::ToString;
use ::self::cmp::{Eq, PartialEq, PartialOrd, Ord, Ordering};
use ::self::hash::{Hash, Hasher};
use ::self::primitives::clone::Clone;
use ::self::primitives::default::Default;

impl UInt16 {
    pub fn reverse_bits(self) -> UInt16 {
        return ::self::intrinsic::reverse_bits(<UInt16>, self);
    }

    pub fn rotate_left(self, n: UInt32) -> UInt16 {
        return ::self::intrinsic::rotate_left(<UInt16>, self, n);
    }

    pub fn rotate_right(self, n: UInt32) -> UInt16 {
        return ::self::intrinsic::rotate_right(<UInt16>, self, n);
    }

    pub fn bit_and(self, n: UInt16) -> UInt16 {
        return ::self::intrinsic::bit_and(<UInt16>, self, n);
    }

    pub fn bit_or(self, n: UInt16) -> UInt16 {
        return ::self::intrinsic::bit_or(<UInt16>, self, n);
    }

    pub fn bit_xor(self, n: UInt16) -> UInt16 {
        return ::self::intrinsic::bit_xor(<UInt16>, self, n);
    }

    pub fn bit_not(self) -> UInt16 {
        return ::self::intrinsic::bit_not(<UInt16>, self);
    }

    pub fn bit_shl(self, n: UInt32) -> UInt16 {
        return ::self::intrinsic::bit_shl(<UInt16>, self, n);
    }

    pub fn bit_shr(self, n: UInt32) -> UInt16 {
        return ::self::intrinsic::bit_shr(<UInt16>, self, n);
    }
    pub fn min_value() -> UInt16 {
        return 0;
    }
    pub fn max_value() -> UInt16 {
        return 65535_u16;
    }

    pub fn wrapping_add(self, rhs: UInt16) -> UInt16 {
        return self + rhs;
    }

    pub fn wrapping_sub(self, rhs: UInt16) -> UInt16 {
        return self - rhs;
    }

    pub fn wrapping_mul(self, rhs: UInt16) -> UInt16 {
        return self * rhs;
    }

    pub fn checked_add(self, rhs: UInt16) -> Option<UInt16> {
        let sum = self + rhs;
        if sum < self {
            return None;
        }
        return Some(sum);
    }

    pub fn checked_sub(self, rhs: UInt16) -> Option<UInt16> {
        if self < rhs {
            return None;
        }
        return Some(self - rhs);
    }

    pub fn checked_mul(self, rhs: UInt16) -> Option<UInt16> {
        if self == 0 {
            return Some(0);
        }
        let prod = self * rhs;
        if prod / self != rhs {
            return None;
        }
        return Some(prod);
    }

    pub fn saturating_add(self, rhs: UInt16) -> UInt16 {
        match self.checked_add(rhs) {
            Some(v) => {
                return v;
            }
            None => {
                return UInt16::max_value();
            }
        }
    }

    pub fn saturating_sub(self, rhs: UInt16) -> UInt16 {
        match self.checked_sub(rhs) {
            Some(v) => {
                return v;
            }
            None => {
                return UInt16::min_value();
            }
        }
    }

    pub fn saturating_mul(self, rhs: UInt16) -> UInt16 {
        match self.checked_mul(rhs) {
            Some(v) => {
                return v;
            }
            None => {
                return UInt16::max_value();
            }
        }
    }
}

impl ToString for UInt16 {
    fn to_string_inplace(&self, buf: &mut String) {
        let m = *self as UInt64;
        m.to_string_inplace(buf);
    }

    fn to_string(&self) -> String {
        let mut buf = String::new();
        self.to_string_inplace(&mut buf);
        return buf;
    }
}

impl PartialEq for UInt16 {
    fn eq(&self, other: &Self) -> Bool {
        return *self == *other;
    }

    fn ne(&self, other: &Self) -> Bool {
        return *self != *other;
    }
}

impl Eq for UInt16 {}

impl PartialOrd for UInt16 {
    fn partial_cmp(&self, other: &Self) -> Option<Ordering> {
        if *self < *other {
            return Some(Ordering::Less);
        }
        else if *self == *other {
            return Some(Ordering::Equal);
        }
        else {
            return Some(Ordering::Greater);
        }
    }
    fn lt(&self, other: &Self) -> Bool {
        return self.partial_cmp(other) is Some(Ordering::Less);
    }
    fn le(&self, other: &Self) -> Bool {
        return !(self.partial_cmp(other) is Some(Ordering::Greater));
    }
    fn gt(&self, other: &Self) -> Bool {
        return self.partial_cmp(other) is Some(Ordering::Greater);
    }
    fn ge(&self, other: &Self) -> Bool {
        return !(self.partial_cmp(other) is Some(Ordering::Less));
    }
}

impl Ord for UInt16 {
    fn cmp(&self, other: &Self) -> Ordering {
        if *self < *other {
            return Ordering::Less;
        }
        else if *self == *other {
            return Ordering::Equal;
        }
        else {
            return Ordering::Greater;
        }
    }
}

impl Hash for UInt16 {
    fn hash(&self, state: &mut Hasher) {
        state.combine(*self as UInt64);
    }
}

impl Clone for UInt16 {
    fn clone(&self) -> UInt16 {
        return *self;
    }
}

impl Default for UInt16 {
    fn default() -> UInt16 {
        return 0;
    }
}
